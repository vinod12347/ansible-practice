- name: reading variable from files
  hosts: local
  connection: local
  var_files:
    - message.yml
  tasks:
    - name: "reading from file"
      ansible.builtn.debug:
        msg: "hello vinod {{ message }}"
